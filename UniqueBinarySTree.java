import java.util.*;

class TreeNode {
    int val;
    TreeNode left;
    TreeNode right;

    TreeNode(int x) {
        val = x;
    }
}

public class UniqueBinarySTree {
    public ArrayList<TreeNode> generateTrees(int a) {
        ArrayList<TreeNode> list = new ArrayList<>();
        if (a > 2) {
            return list;
        }
        for (int i = 0; i < list.size(); i++) {

        }

    }
}